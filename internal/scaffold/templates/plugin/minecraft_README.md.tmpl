# {{.Name}}

{{.Name}} 是一个 AnyBot SDK Minecraft 群管插件起点。它演示了管理员命令、群范围限制、玩家绑定表和 `ctx.DataDir()` 私有文件存储。

## 开发

```sh
go test ./...
```

## 本地安装

在机器人工作目录中执行：

```sh
anybot plugin add {{.Module}} -name {{.Manifest}} -replace <插件目录>
anybot plugin enable {{.Manifest}}
anybot up
```

## 配置

```yaml
enabled: true
config:
  command: {{.Command}}
  server_name: survival
  admins: []
  allowed_groups: []
```

- `admins`：为空时使用宿主 `security.superusers`；非空时只允许这些用户管理绑定。
- `allowed_groups`：为空时不限制群聊；填群号后只在这些群响应。

## 命令

```text
/{{.Command}} bind <QQ> <玩家名>
/{{.Command}} who <玩家名>
/{{.Command}} list
```

绑定表会保存到插件私有数据目录的 `bindings.json`。接入真实 Minecraft 服务端时，可以在保存绑定后调用 RCON、HTTP bridge 或队列，把白名单、封禁和身份同步下发到游戏服。
